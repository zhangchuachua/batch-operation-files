{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "batch-file-operation",
  "title": "Batch File Operation",
  "description": "A powerful tool for batch file operations and JSON modifications",
  "icon": "command-icon.png",
  "author": "zwx",
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "copy",
      "title": "Copy Files",
      "description": "Copy files with regex pattern support",
      "mode": "view"
    },
    {
      "name": "modify-json",
      "title": "Modify JSON",
      "description": "Modify JSON files using JSONPath expressions",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.64.5"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.6",
    "@types/node": "^20.8.10",
    "@types/react": "^18.2.27",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
